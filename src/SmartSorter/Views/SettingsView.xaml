<UserControl x:Class="SmartSorter.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <!-- 💡 Grid 대신 Border에 Padding을 주거나 Margin을 활용합니다. -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 1. 타이틀 영역 -->
            <DockPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="⚙️ 시스템 및 통신/각도 환경설정" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
            </DockPanel>

            <!-- 2. 설정 항목 카드 영역 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- [좌측] PLC / RS-485 통신 설정 -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Color="#CCCCCC" Direction="270" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="🔌 Modbus RS-485 통신 설정" FontSize="15" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15"/>

                        <TextBlock Text="시리얼 COM 포트" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ComPort, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10,6" FontSize="13" Margin="0,0,0,15"/>

                        <TextBlock Text="보레이트 (Baud Rate)" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding BaudRate, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10,6" FontSize="13" Margin="0,0,0,15"/>

                        <!-- 💡 [NEW] PLC 국번 (Station ID) 추가 영역 -->
                        <TextBlock Text="PLC 국번 (Station ID)" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding StationId, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="10,6" FontSize="13" Margin="0,0,0,15"/>
                    </StackPanel>
                </Border>

                <!-- [우측] 색상별 서보모터 분류 각도 설정 -->
                <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Color="#CCCCCC" Direction="270" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="🎯 색상별 서보모터 타겟 각도 (°)" FontSize="15" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔴 Red 각도:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#DC3545"/>
                            <TextBox Grid.Column="1" Text="{Binding RedServoAngle}" Height="32" Padding="8,4"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🟢 Green 각도:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#198754"/>
                            <TextBox Grid.Column="1" Text="{Binding GreenServoAngle}" Height="32" Padding="8,4"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔵 Blue 각도:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#0D6EFD"/>
                            <TextBox Grid.Column="1" Text="{Binding BlueServoAngle}" Height="32" Padding="8,4"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🟡 Yellow 각도:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#FFC107"/>
                            <TextBox Grid.Column="1" Text="{Binding YellowServoAngle}" Height="32" Padding="8,4"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- 3. 저장 버튼 -->
            <Button Grid.Row="2" Content="💾 설정값 DB 저장" Command="{Binding SaveSettingsCommand}" 
                    Height="45" Width="180" HorizontalAlignment="Right" Margin="0,15,0,0"
                    Style="{StaticResource PrimaryButtonStyle}" FontSize="14"/>
        </Grid>
    </ScrollViewer>
</UserControl>